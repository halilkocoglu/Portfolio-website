ErrorDocument 404 /index.html

<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Eğer istenen şey gerçekten bir dosya veya klasörse dokunma
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
</IfModule>